# Optional dependencies for the AI-image detection module (beta).
# The main app runs fine without these; the /api/detect endpoint reports
# "unavailable" until they are installed:
#   pip install -r detection/requirements.txt
numpy>=1.24,<3
scipy>=1.10,<2
Pillow>=10.0.0,<13
PyWavelets>=1.4,<2
invisible-watermark>=0.2.0,<1
