.venv/
__pycache__/
*.py[cod]

node_modules/
dist/
frontend/dist/

*.log
frontend-smoke*.png

# Local provider config holds real API keys — keep it out of git.
# A template (configs.example.json) is committed; the backend auto-creates
# backend/data/configs.json on first run.
backend/data/configs.json
